import SettingsPlaceholder from "../_components/settings-placeholder";

export default function ChangeEmailPage() {
  return (
    <SettingsPlaceholder
      title="Change Email"
      description="Change your email address and confirm the new address before saving changes."
    />
  );
}
